cmd / server-starter .env connect файл до базы данных (просто введи пароль драйвер к
 примеру 1234 postgres
)
после подключения к базе данных нужно внутри нее создать  таблицу в моем случае mybd с колонкой env
в которую будет сохраняться сама env 
в результате по переходу по ссылке  localhost:8080/env/prod или /env/debug переменная env 
будет меняться тем самым меня регистр ошибок (logger) с debug на info или наоборот

▎Описание проекта: Dynamic Environment Switcher

Цель проекта:  
Dynamic Environment Switcher — это инструмент, который автоматически переключает окружение приложения (например, между local и info) в зависимости от URL,
по которому происходит GET-запрос. Это решение значительно упрощает процесс тестирования и отладки, обеспечивая более гибкую и эффективную работу с различными средами разработки.

---

Ключевые особенности:

1. Автоматическое переключение окружения:  
   При переходе на определенные ссылки приложение динамически меняет параметры окружения (env), позволяя разработчикам и тестировщикам легко 
переключаться между локальной и продакшен средами без необходимости вручную изменять конфигурацию.

2. Интеграция с базой данных:  
   Проект использует базу данных для хранения параметров окружения и их соответствия определенным URL. Это обеспечивает легкость в управлении
настройками и возможность добавления новых окружений или ссылок без изменения кода приложения.

3. Упрощение отладки и тестирования:  
   Благодаря автоматическому переключению окружения, разработчики могут быстрее тестировать изменения, не теряя времени на конфигурацию.
Это особенно полезно в командах с несколькими разработчиками, где каждый может работать с различными средами.

4. Логирование:  
   Проект включает систему логирования, которая меняет уровень логов в зависимости от текущего окружения. Это позволяет легко отслеживать
ошибки и производительность как в локальной, так и в продакшен среде, улучшая качество кода и уменьшая время на поиск проблем.

5. Гибкость и масштабируемость:  
   Система позволяет легко добавлять новые параметры окружения и URL, что делает проект масштабируемым и адаптируемым под различные потребности бизнеса.

---

Значимость проекта:  
Dynamic Environment Switcher решает важную задачу, связанную с управлением окружениями в процессе разработки и тестирования программного обеспечения. 
Он улучшает производительность команды, снижает вероятность ошибок при переключении окружений и обеспечивает более высокое качество конечного продукта.
Внедрение данного решения может значительно ускорить цикл разработки и повысить удовлетворенность пользователей за счет более стабильной работы приложений.

---

